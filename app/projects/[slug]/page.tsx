import Link from'next/link';import{projects}from'../../../lib/data';import WebverseScene from'../../../components/WebverseScene';import{Nav,Footer}from'../../../components/SiteChrome';export function generateStaticParams(){return projects.map(([slug])=>({slug}))}export default function Detail({params}:{params:{slug:string}}){const p=projects.find(x=>x[0]===params.slug);if(!p)return <main>Not found</main>;return <><Nav/><main className="page"><div className="scene"><WebverseScene variant="projects"/></div><p className="eyebrow">PROJECT CASE STUDY</p><h1>{p[1]}<br/><i>UNIVERSE.</i></h1><article className="resume"><p>{p[2]}</p><h3>Role</h3><p>Full-Stack Web Developer · Product UI/UX · API Integration</p><h3>What I built</h3><p>Modern responsive interface, production-ready web flows, API/data integration, and a visually immersive experience designed around the product&apos;s users.</p><Link className="button" href="/projects">← ALL PROJECTS</Link></article></main><Footer/></>}