import Link from'next/link';import WebverseScene from'../../components/WebverseScene';import{Nav,Footer}from'../../components/SiteChrome';import{projects}from'../../lib/data';export default function Projects(){return <><Nav/><main className="page"><div className="scene"><WebverseScene variant="projects"/></div><p className="eyebrow">01 — SELECTED PROJECT UNIVERSE</p><h1>THINGS I<br/><i>BUILT.</i></h1><div className="cards">{projects.map(([slug,name,tag],i)=><Link className="card" href={`/projects/${slug}`} key={slug}><span>0{i+1}</span><h2>{name}</h2><p>{tag}</p><b>OPEN CASE STUDY ↗</b></Link>)}</div></main><Footer/></>}